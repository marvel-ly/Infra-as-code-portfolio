name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Print Deployment Message
        run: echo "✅ Build succeeded! Simulated deployment triggered."

      - name: List files in app folder
        run: ls -l ./app
